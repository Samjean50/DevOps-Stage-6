name: Infrastructure Deployment

on:
  push:
    branches: [main]
    paths:
      - 'infra/terraform/**'
      - 'infra/ansible/**'
      - '.github/workflows/infrastructure.yml'
  workflow_dispatch:

env:
  AWS_REGION: us-east-1
  TF_VERSION: 1.6.0

jobs:
  terraform-plan:
    name: Terraform Plan & Drift Detection
    runs-on: ubuntu-latest
    outputs:
      has_drift: ${{ steps.drift.outputs.has_drift }}
      plan_exitcode: ${{ steps.plan.outputs.exitcode }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Create terraform.tfvars
        working-directory: infra/terraform
        run: |
          cat > terraform.tfvars <<EOF
          aws_region           = "${{ env.AWS_REGION }}"
          project_name         = "todo-app"
          environment          = "production"
          instance_type        = "t3.medium"
          key_name             = "${{ secrets.AWS_KEY_NAME }}"
          ssh_user             = "ubuntu"
          ssh_private_key_path = "~/.ssh/deploy_key"
          domain_name          = "${{ secrets.DOMAIN_NAME }}"
          jwt_secret           = "${{ secrets.JWT_SECRET }}"
          acme_email           = "${{ secrets.ACME_EMAIL }}"
          github_repo          = "${{ github.server_url }}/${{ github.repository }}.git"
          github_branch        = "${{ github.ref_name }}"
          EOF

      - name: Terraform Init
        working-directory: infra/terraform
        run: terraform init

      - name: Terraform Validate
        working-directory: infra/terraform
        run: terraform validate

      - name: Terraform Plan
        id: plan
        working-directory: infra/terraform
        run: |
          terraform plan -detailed-exitcode -out=tfplan -no-color 2>&1 | tee plan_output.txt
          echo "exitcode=$?" >> $GITHUB_OUTPUT
        continue-on-error: true

      - name: Detect Drift
        id: drift
        working-directory: infra/terraform
        run: |
          if [ "${{ steps.plan.outputs.exitcode }}" == "2" ]; then
            echo "has_drift=true" >> $GITHUB_OUTPUT
            echo "⚠️ Infrastructure drift detected!"
          else
            echo "has_drift=false" >> $GITHUB_OUTPUT
            echo "✅ No infrastructure drift detected"
          fi

      - name: Format plan output for email
        if: steps.drift.outputs.has_drift == 'true'
        id: format_plan
        working-directory: infra/terraform
        run: |
          echo "PLAN_OUTPUT<<EOF" >> $GITHUB_ENV
          cat plan_output.txt | head -n 200
          echo "EOF" >> $GITHUB_ENV

      - name: Send Drift Alert Email
        if: steps.drift.outputs.has_drift == 'true'
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          secure: true
          username: ${{ secrets.EMAIL_USERNAME }}
          password: ${{ secrets.EMAIL_PASSWORD }}
          subject: "🚨 Terraform Drift Detected - TODO App Infrastructure"
          to: ${{ secrets.ALERT_EMAIL }}
          from: DevOps Alerts <${{ secrets.EMAIL_USERNAME }}>
          html_body: |
            <!DOCTYPE html>
            <html>
            <head>
                <style>
                    body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
                    .header { background: #ff6b6b; color: white; padding: 20px; text-align: center; }
                    .content { padding: 20px; background: #f4f4f4; }
                    .alert-box { background: #fff3cd; border-left: 4px solid #ffc107; padding: 15px; margin: 20px 0; }
                    .details { background: white; padding: 15px; margin: 10px 0; border-radius: 5px; }
                    pre { background: #2d2d2d; color: #f8f8f8; padding: 15px; overflow-x: auto; border-radius: 5px; }
                    .button { background: #007bff; color: white; padding: 12px 24px; text-decoration: none; border-radius: 5px; display: inline-block; margin: 10px 0; }
                    .footer { text-align: center; padding: 20px; color: #666; font-size: 12px; }
                </style>
            </head>
            <body>
                <div class="header">
                    <h1>🚨 Infrastructure Drift Detected</h1>
                </div>
                <div class="content">
                    <div class="alert-box">
                        <strong>⚠️ Action Required:</strong> Infrastructure changes have been detected and require your approval before deployment.
                    </div>
                    
                    <div class="details">
                        <h3>📋 Deployment Details</h3>
                        <ul>
                            <li><strong>Repository:</strong> ${{ github.repository }}</li>
                            <li><strong>Branch:</strong> ${{ github.ref_name }}</li>
                            <li><strong>Commit:</strong> ${{ github.sha }}</li>
                            <li><strong>Triggered by:</strong> ${{ github.actor }}</li>
                            <li><strong>Workflow:</strong> ${{ github.workflow }}</li>
                        </ul>
                    </div>

                    <div class="details">
                        <h3>📊 Terraform Plan Summary</h3>
                        <pre>${{ env.PLAN_OUTPUT }}</pre>
                    </div>

                    <div style="text-align: center; margin: 30px 0;">
                        <a href="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}" class="button">
                            🔍 Review Changes & Approve Deployment
                        </a>
                    </div>

                    <div class="alert-box">
                        <strong>ℹ️ Next Steps:</strong>
                        <ol>
                            <li>Review the Terraform plan above</li>
                            <li>Click the button to go to GitHub Actions</li>
                            <li>Approve the deployment if changes are expected</li>
                            <li>The infrastructure will be updated automatically after approval</li>
                        </ol>
                    </div>
                </div>
                <div class="footer">
                    <p>This is an automated alert from your DevOps pipeline</p>
                    <p>Time: ${{ github.event.head_commit.timestamp }}</p>
                </div>
            </body>
            </html>

      - name: Upload Plan Artifact
        if: steps.drift.outputs.has_drift == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: tfplan
          path: |
            infra/terraform/tfplan
            infra/terraform/plan_output.txt
          retention-days: 5

  await-approval:
    name: Wait for Manual Approval
    needs: terraform-plan
    if: needs.terraform-plan.outputs.has_drift == 'true'
    runs-on: ubuntu-latest
    environment:
      name: production
    
    steps:
      - name: Manual Approval Required
        run: |
          echo "⏳ Waiting for manual approval to proceed with Terraform apply"
          echo "Review the plan and approve in the GitHub Actions UI"

  terraform-apply:
    name: Terraform Apply
    needs: [terraform-plan, await-approval]
    if: |
      always() && 
      needs.terraform-plan.result == 'success' &&
      (needs.await-approval.result == 'success' || needs.terraform-plan.outputs.has_drift == 'false')
    runs-on: ubuntu-latest
    outputs:
      server_ip: ${{ steps.outputs.outputs.server_ip }}
      server_id: ${{ steps.outputs.outputs.server_id }}
    
    steps:
      - name: Run Ansible Deployment
        if: success()
        run: |
          echo "Running Ansible Deployment..."
        
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Ansible
        run: |
          pip install ansible
          ansible-galaxy collection install community.docker

      - name: Setup SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          
          # Start SSH agent and add key
          eval "$(ssh-agent -s)"
          ssh-add ~/.ssh/deploy_key
          
          # Add server to known hosts
          ssh-keyscan -H ${{ needs.terraform-apply.outputs.server_ip }} >> ~/.ssh/known_hosts

      - name: Test SSH connection
        run: |
          ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no ubuntu@${{ needs.terraform-apply.outputs.server_ip }} 'echo "SSH connection successful"'

      - name: Wait for instance to be ready
        run: |
          echo "⏳ Waiting for instance to be fully ready..."
          sleep 30

      - name: Create Ansible inventory
        run: |
          mkdir -p infra/ansible/inventory
          cat > infra/ansible/inventory/hosts <<EOF
          [app_servers]
          ${{ needs.terraform-apply.outputs.server_ip }} ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/deploy_key ansible_ssh_common_args='-o StrictHostKeyChecking=no'

          [app_servers:vars]
          domain_name=${{ secrets.DOMAIN_NAME }}
          jwt_secret=${{ secrets.JWT_SECRET }}
          acme_email=${{ secrets.ACME_EMAIL }}
          github_repo=${{ github.server_url }}/${{ github.repository }}.git
          github_branch=${{ github.ref_name }}
          EOF

      - name: Deploy application with Ansible
        working-directory: infra/ansible
        env:
          ANSIBLE_HOST_KEY_CHECKING: False
        run: |
          ansible-playbook -i inventory/hosts playbooks/deploy.yml -v

      - name: Verify deployment
        run: |
          echo "⏳ Waiting for application to start..."
          sleep 60
          
          echo "🔍 Testing application endpoints..."
          if curl -k -s --max-time 10 https://${{ secrets.DOMAIN_NAME }} > /dev/null 2>&1; then
            echo "✅ Application is accessible at https://${{ secrets.DOMAIN_NAME }}"
          else
            echo "⚠️ Application may still be starting..."
          fi

  send-success-notification:
    name: Send Success Notification
    needs: [terraform-apply]
    if: success()
    runs-on: ubuntu-latest
    
    steps:
      - name: Send Success Email
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          secure: true
          username: ${{ secrets.EMAIL_USERNAME }}
          password: ${{ secrets.EMAIL_PASSWORD }}
          subject: "✅ TODO App Infrastructure Deployed Successfully"
          to: ${{ secrets.ALERT_EMAIL }}
          from: DevOps Alerts <${{ secrets.EMAIL_USERNAME }}>
          html_body: |
            <!DOCTYPE html>
            <html>
            <head>
                <style>
                    body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
                    .header { background: #28a745; color: white; padding: 20px; text-align: center; }
                    .content { padding: 20px; background: #f4f4f4; }
                    .success-box { background: #d4edda; border-left: 4px solid #28a745; padding: 15px; margin: 20px 0; }
                    .details { background: white; padding: 15px; margin: 10px 0; border-radius: 5px; }
                    .button { background: #007bff; color: white; padding: 12px 24px; text-decoration: none; border-radius: 5px; display: inline-block; margin: 10px 0; }
                </style>
            </head>
            <body>
                <div class="header">
                    <h1>✅ Deployment Successful</h1>
                </div>
                <div class="content">
                    <div class="success-box">
                        <strong>🎉 Success:</strong> Your TODO application infrastructure has been deployed successfully!
                    </div>
                    
                    <div class="details">
                        <h3>📋 Deployment Information</h3>
                        <ul>
                            <li><strong>Application URL:</strong> <a href="https://${{ secrets.DOMAIN_NAME }}">https://${{ secrets.DOMAIN_NAME }}</a></li>
                            <li><strong>Repository:</strong> ${{ github.repository }}</li>
                            <li><strong>Branch:</strong> ${{ github.ref_name }}</li>
                            <li><strong>Commit:</strong> ${{ github.sha }}</li>
                            <li><strong>Deployed by:</strong> ${{ github.actor }}</li>
                        </ul>
                    </div>

                    <div style="text-align: center; margin: 30px 0;">
                        <a href="https://${{ secrets.DOMAIN_NAME }}" class="button">🚀 Visit Application</a>
                        <a href="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}" class="button">📊 View Workflow</a>
                    </div>
                </div>
            </body>
            </html>

  send-failure-notification:
    name: Send Failure Notification
    needs: [terraform-plan, terraform-apply]
    if: failure()
    runs-on: ubuntu-latest
    
    steps:
      - name: Send Failure Email
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          secure: true
          username: ${{ secrets.EMAIL_USERNAME }}
          password: ${{ secrets.EMAIL_PASSWORD }}
          subject: "❌ TODO App Infrastructure Deployment Failed"
          to: ${{ secrets.ALERT_EMAIL }}
          from: DevOps Alerts <${{ secrets.EMAIL_USERNAME }}>
          html_body: |
            <!DOCTYPE html>
            <html>
            <head>
                <style>
                    body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
                    .header { background: #dc3545; color: white; padding: 20px; text-align: center; }
                    .content { padding: 20px; background: #f4f4f4; }
                    .error-box { background: #f8d7da; border-left: 4px solid #dc3545; padding: 15px; margin: 20px 0; }
                    .details { background: white; padding: 15px; margin: 10px 0; border-radius: 5px; }
                    .button { background: #007bff; color: white; padding: 12px 24px; text-decoration: none; border-radius: 5px; display: inline-block; margin: 10px 0; }
                </style>
            </head>
            <body>
                <div class="header">
                    <h1>❌ Deployment Failed</h1>
                </div>
                <div class="content">
                    <div class="error-box">
                        <strong>⚠️ Error:</strong> Infrastructure deployment encountered an error.
                    </div>
                    
                    <div class="details">
                        <h3>📋 Failure Details</h3>
                        <ul>
                            <li><strong>Repository:</strong> ${{ github.repository }}</li>
                            <li><strong>Branch:</strong> ${{ github.ref_name }}</li>
                            <li><strong>Commit:</strong> ${{ github.sha }}</li>
                            <li><strong>Triggered by:</strong> ${{ github.actor }}</li>
                        </ul>
                    </div>

                    <div style="text-align: center; margin: 30px 0;">
                        <a href="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}" class="button">
                            🔍 View Workflow Logs
                        </a>
                    </div>
                </div>
            </body>
            </html>